{
  "name": "meshy-ui",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev":   "yarn install --frozen-lockfile && next dev",
    "build": "yarn install --frozen-lockfile && next build",
    "start": "yarn install --frozen-lockfile && next start",
    "examples": "node ./examples/init.js"
  },
  "dependencies": {
    "next": "^10.0.9",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-hook-form": "^7.4.0"
  }
}
